Flow 1

URL abbv - NJ

BU - Clinical Laboratory

BU abbv - CLAB


import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
  const entityName = "CL_25-06-2026_07:05:55 PM";
  await page.goto("http://172.16.3.2/ALiSNJDOH2TESTING11.4.38.08/LoginNJ.aspx");
  await page.getByRole('textbox', { name: 'Login Name' }).fill("admin");
  await page.getByRole('textbox', { name: 'Password' }).fill("password");
  await page.getByRole('link', { name: 'Login', exact: true }).click();

  await page.getByRole('link', { name: 'Licensing' }).hover();
  await page.getByRole('link', { name: 'Business Entity' }).hover();
  await page.getByRole('link', { name: 'New' }).click();

  await page.getByRole('textbox', { name: "-- Choose One --", exact: true }).click();
  await page.getByRole('option', { name: "Clinical Laboratory", exact: true }).click();
  await page.getByText("License Type", { exact: true }).click();
  await page.getByText("Aerobic", { exact: true }).click();
  await page.getByText("Erythropoietin").click();
  await page.getByText("Prealbumin").click();
  await page.getByText("Zinc").click();
  await page.getByText("SCL-").click();
  await page.getByRole('button', { name: "Ok", exact: true }).click();
  await page.locator("textarea").fill("Test Clinical Lab");
  await page.getByRole('textbox', { name: "Laboratory Name" }).fill(entityName);
  await page.getByRole('textbox', { name: "Registered Legal Business Name" }).fill(entityName);
  await page.getByRole('textbox', { name: "Federal Tax ID#" }).fill("4345165007");
  await page.getByLabel("Ownership Type").selectOption("IND");
  await page.getByRole('textbox', { name: "First Name" }).fill("Linda");
  await page.getByRole('textbox', { name: "Last Name" }).fill("Taylor");
  await page.getByLabel("Role").selectOption("ADM");
  await page.getByRole('textbox', { name: "Email" }).fill("mohdatif.jamal@ops1.advancedgrc.com");
  await page.locator("#txtPhone").fill("730-942-9262");
  await page.locator("#txtAddress_MLG").fill("482 Market Street");
  await page.locator("#lblApt_MLG").fill("Apt 56");
  await page.locator("#txtCity_MLG").fill("Dallas");
  await page.locator("#txtZip_MLG").fill("67099-7079");
  await page.locator("#ddlCounty_MLG").selectOption("07");
  await page.locator("#txtPrimaryPhone_MLG").fill("395-527-7479");
  await page.locator("#txtPrimaryEmail_MLG").fill("mohdatif.jamal@ops1.advancedgrc.com");
  await page.locator("#txtAlternateEmail_MLG").fill("mohdatif.jamal@ops1.advancedgrc.com");
  await page.locator("#ddlCopy_PHL").selectOption("MLG");
  // Framework selects all visible County dropdowns with value "07".
  await page.locator("#ddlCounty_MLG").selectOption("07");
  // Framework selects all visible County dropdowns with value "07".
  // No actions configured.
  await page.getByRole('link', { name: 'Save' }).or(page.getByRole('button', { name: 'Save' })).click();

  // After saving, framework opens Modify and searches the created entity.

  await page.getByRole('link', { name: 'Licensing' }).hover();
  await page.getByRole('link', { name: 'Business Entity' }).hover();
  await page.getByRole('link', { name: 'Modify' }).click();

  await page.locator('#select2-ddlProgramInternal-container').click();
  await page.getByRole('option', { name: "Clinical Laboratory" }).click();
  await page.getByRole('textbox', { name: "Facility Name (Legal Name)" }).fill(entityName);
  await page.getByRole('button', { name: 'Search' }).click();
  await page.getByRole('link', { name: entityName }).click();

  await page.getByText("Entity Information", { exact: true }).click();
  await page.getByText("Owner(s)").click();
  await page.getByText("Personnel(s)").click();
  await page.getByText("Additional Information").click();
  await page.getByText("Activity Log(s)").click();

});

// ============================================================
// Framework Precondition Test Steps
// ============================================================
// Execute these only when the related tab/section table column headers are not displayed.
// Source: businessUnit.tableHeaderPreconditions from the current ALiS_Baseline framework.

// Owner(s)
// Precondition name: CLab owner row
await page.getByRole('link', { name: "Add", exact: true }).click();
await page.getByRole('textbox', { name: "Last Name" }).fill("Jamal");
await page.getByRole('textbox', { name: "First Name" }).fill("Atif");
await page.getByRole('button', { name: "Save", exact: true }).click();

// Personnel(s)
// Precondition name: CLab personnel row
await page.getByRole('link', { name: "Add", exact: true }).click();
await page.getByRole('textbox', { name: "Last Name" }).fill("Jamal");
await page.getByRole('textbox', { name: "First Name" }).fill("Atif");
await page.getByLabel("Employment Type").selectOption("FT");
await page.getByRole('checkbox', { name: "General Supervisor", exact: true }).check();
await page.getByRole('button', { name: "Save", exact: true }).click();

// Activity Log(s)
// Precondition name: CLab activity log row
await page.getByRole('link', { name: "Add", exact: true }).click();
await page.getByLabel("Action Code").selectOption("INR");
await page.locator(".angular-editor-textarea").fill("Test");
await page.getByRole('button', { name: "Save", exact: true }).click();
